#' Data frame containing probe set IDs and quality strings
#' 
#' The \code{psets_and_qstrings} object contains a data frame with
#' two columns. The first one contains some probe set IDs and the 
#' second one contains the relative quality strings. This data frame
#' is the result of the application of \code{add_quality_string} to 
#' the pre-processed output of \code{bowtie}.
#' @docType data
#' @keywords datasets
#' @name psets_and_qstrings
#' @usage psets_and_qstrings
#' @format data frame with 417 rows and 2 columns
NULL